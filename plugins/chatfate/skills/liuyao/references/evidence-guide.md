# 六爻依据与来源

calculated/traditional 段引用本次 calculatedFacts；user 段引用 userContext 或 normalizedInput.question/topic/subject；practical 段可以没有引用，不用卦盘为常识背书。

重点路径包括 baseHexagram、changedHexagram、ganZhiTime、lines、movingLinePositions、shiLinePosition、yingLinePosition、useGod.groups、hiddenSpirits、timingTriggers。不存在的变卦、变爻、应期与候选不要补写；数组位置以返回数据为准。

爻的 strength、monthDayInfluence、voidState、movementState、changedLine 分别表达不同条件，不能把其中一项替代整个爻的综合判断。用神 selected 未决时保留不确定，不替引擎修改选择。

引用 useGod 并不能证明他人心意；引用父母爻并不能证明文档实际缺失。先说明传统映射，再另用真实资料核对。timingTriggers 只给触发条件，没有公历日程时不推算日期。

引用数组可用点号下标或数字方括号；逐段核对具体卦名、干支、爻位与关系。source=calculated 只复述计算资料，不夹带结果推论。路径可解析并不代表文字推理成立。
